name: iOS Build
on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate
        working-directory: ${{ github.workspace }}/Sources/WorkShiftAlarm   # 修改路径为 Sources/WorkShiftAlarm

      - name: Build (no signing)
        run: |
          xcodebuild \
            -project ${{ github.workspace }}/Sources/WorkShiftAlarm/WorkShiftAlarm.xcodeproj \
            -scheme WorkShiftAlarm \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            build
        working-directory: ${{ github.workspace }}/Sources/WorkShiftAlarm   # 修改路径为 Sources/WorkShiftAlarm

      - name: Archive
        run: |
          xcodebuild \
            -project ${{ github.workspace }}/Sources/WorkShiftAlarm/WorkShiftAlarm.xcodeproj \
            -scheme WorkShiftAlarm \
            -configuration Release \
            -sdk iphoneos \
            -archivePath $PWD/build/WorkShiftAlarm.xcarchive \
            archive
        working-directory: ${{ github.workspace }}/Sources/WorkShiftAlarm   # 修改路径为 Sources/WorkShiftAlarm

      - name: Export IPA (requires signing)
        run: |
          xcodebuild \
            -exportArchive \
            -archivePath $PWD/build/WorkShiftAlarm.xcarchive \
            -exportPath $PWD/build \
            -exportOptionsPlist ${{ github.workspace }}/Sources/WorkShiftAlarm/ExportOptions.plist   # 修改路径为 Sources/WorkShiftAlarm
        working-directory: ${{ github.workspace }}/Sources/WorkShiftAlarm   # 修改路径为 Sources/WorkShiftAlarm

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: WorkShiftAlarm-ipa
          path: ${{ github.workspace }}/Sources/WorkShiftAlarm/build/*.ipa   # 修改路径为 Sources/WorkShiftAlarm
