name: iOS Build
on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate
        working-directory: ${{ github.workspace }}/WorkShiftAlarm  # 使用 GitHub 工作空间来确保路径正确

      - name: Build (no signing)
        run: |
          xcodebuild \
            -project ${{ github.workspace }}/WorkShiftAlarm/WorkShiftAlarm.xcodeproj \
            -scheme WorkShiftAlarm \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            build
        working-directory: ${{ github.workspace }}/WorkShiftAlarm  # 使用 GitHub 工作空间来确保路径正确

      - name: Archive
        run: |
          xcodebuild \
            -project ${{ github.workspace }}/WorkShiftAlarm/WorkShiftAlarm.xcodeproj \
            -scheme WorkShiftAlarm \
            -configuration Release \
            -sdk iphoneos \
            -archivePath $PWD/build/WorkShiftAlarm.xcarchive \
            archive
        working-directory: ${{ github.workspace }}/WorkShiftAlarm  # 使用 GitHub 工作空间来确保路径正确

      - name: Export IPA (requires signing)
        run: |
          xcodebuild \
            -exportArchive \
            -archivePath $PWD/build/WorkShiftAlarm.xcarchive \
            -exportPath $PWD/build \
            -exportOptionsPlist ${{ github.workspace }}/WorkShiftAlarm/ExportOptions.plist  # 使用 GitHub 工作空间来确保路径正确
        working-directory: ${{ github.workspace }}/WorkShiftAlarm  # 使用 GitHub 工作空间来确保路径正确

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: WorkShiftAlarm-ipa
          path: ${{ github.workspace }}/WorkShiftAlarm/build/*.ipa  # 使用 GitHub 工作空间来确保路径正确
